const CoinIndex_title = { 
    width: '5%',
    textAlign: 'centert',
    fontSize: '0.8rem',
    fontWeight: 'bold',
    backgroundColor: 'transparent'
}

const CoinName_title = {
    width: '20%',
    fontSize: '0.75rem',
    fontWeight: 'bold' 
}

const CoinRank_title = {
    width: '8%',
    fontSize: '0.75rem',
    fontWeight: 'bold'
}

const CoinPrice_title = {
    width: '12%',
    fontSize: '0.75rem',
    fontWeight: 'bold'
}

const CoinHigh_title = {
    width: '16%',
    fontSize: '0.75rem',
    fontWeight: 'bold'
}

const CoinIndex = { 
    width: '5%',
    textAlign: 'centert',
    fontSize: '0.8rem',
}

const CoinName = {
    fontSize: '0.75rem',
    marginTop: 'auto',
    marginBottom: 'auto',
    marginLeft: '0.85rem',
    fontWeight: 'bold',
    color: '#515151'
}

const CoinImage = {
    height: '90px',
    width: '90px',
    marginTop: 'auto',
    marginBottom: 'auto'
}

const CoinImageName = {
    width: '20%',
    display: 'flex',
    textAlign: 'center',
    justifyContent: 'center'
}

const CoinRank = {
    width: '8%',
    fontSize: '0.75rem',
}

const CoinPrice = {
    width: '12%',
    fontSize: '0.75rem',
}

const CoinHigh = {
    width: '16%',
    fontSize: '0.75rem',
    color : 'rgb(7,197,121)'
}

const CoinLow = {
    width: '16%',
    fontSize: '0.75rem',
    color: 'rgb(197,7,7)'
}

const MarketCap = {
    width: '16%',
    fontSize: '0.75rem'
}

export {CoinIndex_title, CoinName_title, CoinRank_title,CoinPrice_title, CoinHigh_title} ; 
export {CoinIndex , CoinImage, CoinName, CoinImageName,CoinRank, CoinPrice, CoinHigh, CoinLow, MarketCap} ; 